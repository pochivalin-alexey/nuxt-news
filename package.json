{
  "name": "nuxt-news",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=dev node ./create-env.js && nuxt",
    "build": "nuxt build && nuxt export",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/dotenv": "^1.4.1",
    "country-list": "^2.2.0",
    "nuxt": "^2.13.0"
  },
  "devDependencies": {}
}
